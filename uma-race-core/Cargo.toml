[package]
name = "uma-race-core"
version = "0.1.0"
edition = "2021"
description = "Clean-room frame-stepped Uma Musume race physics (no GPL sources)"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
